<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox fx:id="mainLayout" xmlns="http://javafx.com/javafx/8.0.25"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="projectzero.fx.ContentPaneController">
    <MenuBar>
        <Menu text="File">
            <MenuItem text="save" />
            <MenuItem text="load" />
        </Menu>
    </MenuBar>
    <HBox fx:id="buttonPane">
        <Button fx:id="addButton" text="Add Class" onAction="#addButtonClick" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
        <Button fx:id="editButton" text="Edit Class" onAction="#editButtonClick" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
        <Button fx:id="deleteButton" text="Delete Class" onAction="#deleteButtonClick" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
    </HBox>
    <StackPane fx:id="graphicsPane">
    </StackPane>

</VBox>
